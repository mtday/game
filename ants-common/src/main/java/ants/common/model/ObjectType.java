package ants.common.model;

/**
 * Defines the different types of objects that are available within a cell on the map.
 */
public enum ObjectType {
    EMPTY,
    FRIENDLY_ANT,
    ENEMY_ANT,
    FOOD,
}
